/*
    ! NUMBERS

    Numbers are integers and decimal values which can do all the arithmetic operations. Let's see some examples of Numbers.

    Declaring Number Data Types
*/
    let age=35;
    const gravity=9.81; // we can const for non-changing values, gravitational constant in m/s2
    let mass=72; //  mass in Kilogram
    const PI=3.14;  // pi  geometrical constant

    // more examples
  const boilingPoint=100; // Temperature in oC, boiling point of water which is constant
  const bodyTemp=37; // oC averege human body temperature, which is constant

  console.log(age, gravity, mass, PI, boilingPoint, bodyTemp);